dbs:
  - path: /data/algodrill.sqlite
    replicas:
      - type: s3
        bucket: ${LITESTREAM_BUCKET}
        path: algodrill
        endpoint: ${LITESTREAM_ENDPOINT}
        region: ${LITESTREAM_REGION}
        access-key-id: ${LITESTREAM_ACCESS_KEY_ID}
        secret-access-key: ${LITESTREAM_SECRET_ACCESS_KEY}
